# Cogent Agent - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Authentication (Required - at least one)
# =============================================================================

# Claude OAuth Token (preferred for Claude Max subscription)
CLAUDE_CODE_OAUTH_TOKEN=

# Anthropic API Key (fallback if OAuth fails)
ANTHROPIC_API_KEY=

# GitHub Token (for repository operations)
GH_TOKEN=

# =============================================================================
# Agent Configuration
# =============================================================================

# Unique identifier for this agent
AGENT_ID=cogent-agent-001

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Git Configuration
# =============================================================================

# Git author name for commits
GIT_AUTHOR_NAME=Cogent Agent

# Git author email for commits
GIT_AUTHOR_EMAIL=agent@cogent.local

# Default branch name (default: main)
GIT_DEFAULT_BRANCH=main

# Auto-commit interval in seconds (0 to disable, default: 300)
GIT_AUTO_COMMIT_INTERVAL=300

# Path to SSH key for Git operations (optional)
# SSH_KEY_PATH=/path/to/id_rsa

# =============================================================================
# Paths
# =============================================================================

# Local path to mount as workspace
WORKSPACE_PATH=./workspace

# Local path to mount as assets (skills, commands, CLAUDE.md)
ASSETS_PATH=./assets

# =============================================================================
# NATS Configuration (usually no need to change)
# =============================================================================

# NATS server URL (internal to docker network)
# NATS_URL=nats://nats:4222
